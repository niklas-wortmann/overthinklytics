# Example env vars for frontend backend selection
# Copy to .env.local or export in your shell before running `npx nx dev overthinklytics`

# Show the floating backend switcher (dev convenience)
NEXT_PUBLIC_SHOW_BACKEND_SWITCHER=1

# Debug info toggle (bottom-left). Shows a small chip that expands with backend details.
# Set to 1/true to enable. Default is off for production safety.
NEXT_PUBLIC_SHOW_DEBUG_TOGGLE=1


# Choose by name (django | kotlin | third). Used if NEXT_PUBLIC_API_BASE_URL is not set.
# Note: "third" is the Next.js backend itself (same origin)
NEXT_PUBLIC_BACKEND=django

# Explicit base URL overrides everything else
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Per-backend URLs (used when NEXT_PUBLIC_BACKEND is set)
NEXT_PUBLIC_DJANGO_URL=http://localhost:8000
NEXT_PUBLIC_KOTLIN_URL=http://localhost:8080
# For completeness; when using "third" we typically use same-origin, so this may be ignored
NEXT_PUBLIC_THIRD_URL=http://localhost:3000/api
